{
	"name": "cf-transform-external-s3-compat-bucket",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"aws4fetch": "^1.0.20",
		"hono": "^4.8.5"
	},
	"devDependencies": {
		"wrangler": "^4.25.0"
	}
}
